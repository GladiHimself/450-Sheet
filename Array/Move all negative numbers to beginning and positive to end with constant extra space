#include <bits/stdc++.h>
#include <iostream>
using namespace std;

void move(vector<int> &arr)
{
    sort(arr.begin(), arr.end());
}

int main()
{

    vector<int> arr = {2, 6, 5, -1, -3, 4, -7, -9};
    move(arr);
    for (int e : arr)
    {
        cout << e << " ";
    }

    return 0;
}
