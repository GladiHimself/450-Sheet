//refer lecture 21 love babbar.

void rotate(int arr[], int n)
{
    int temp[n];
    for(int i = 0; i<n; i++){
        temp[(i+1)%n] = arr[i];
    }
    // int i;
    // for(i = 0; i<n; i++){
    //     arr[i] = temp[i];
    // }
    
    copy(temp, temp+n, arr);
}
